package com.admin.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

import com.admin.dao.VisitLogDao;
import com.admin.domain.VisitLog;
import com.admin.service.VisitLogService;

// 방문자수 통계(가입자 비율)할때 쓰는 Controller
@Controller
@RequestMapping(value="/admin/visitlog")
public class VisitLogController extends AdminController<VisitLog, Long, VisitLogService, VisitLogDao>{

}
